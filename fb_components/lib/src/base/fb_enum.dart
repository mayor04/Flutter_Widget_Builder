///If the type is group we should display
///the children based on the `FbGroupType`
enum FbInputType {
  small,
  expanded,
  ltrb,
  text,
  group,
  color,
  dropDownEnum,
  dropDownMap,
}

enum FbGroupType {
  smallHW,
  multiple,
}

enum DropdownDefault {
  none,
}
